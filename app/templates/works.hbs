<div class="container-fluid">
{{#each model as |work|}}
    <h4> {{work.name}}</h4>
    <h6>vehicle: {{work.vehicle.make}} {{work.vehicle.model}}</h6>
    <h6>on: {{work.date}}</h6>
    <h6> at: {{work.mileage}} miles</h6>

{{/each}}
</div>
