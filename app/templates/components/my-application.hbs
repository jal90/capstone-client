<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          <li>
            {{#link-to "users"}}Users{{/link-to}}
          </li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
          <li>
            {{#link-to "change-password"}}Change Password{{/link-to}}
          </li>
          <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
          <li>
            {{#link-to "sign-up"}}Sign Up{{/link-to}}
          </li>
          <li>
            {{#link-to "sign-in"}}Sign In{{/link-to}}
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>



{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div>

{{!-- This was getting in the way of dynamically rendered templates --}}
{{!-- If the template got too tall, instead of scrolling, the footer would kill it --}}
{{!-- <footer>
  <div class="row">
    <div class="col-xs-3 created-by">
      Created by Jesse Adams-Lukowsky
    </div>
    <div class="col-xs-offset-10 webmaster">
      Contact: <a href="mailto:mezzyjezze@gmail.com">Click to start email</a>
    </div>
  </div>
</footer> --}}
