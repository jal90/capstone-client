<nav class="navbar navbar-default nav-car-pic">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="header-db">
      {{#if isAuthenticated}}
        {{!-- <div class="col-xs-6"> --}}
        {{#link-to "vehicles" class="navbar-brand header-db"}}{{format-user user}}'s Dashboard{{/link-to}}
        {{!-- </div> --}}
      {{else}}
        {{#link-to "application" class="navbar-brand" }}
          <h1 style="display: inline;">Mechanic<span style="color: #FFC000;">Mate</span></h1>{{/link-to}}
      {{/if}}
    </div>

    <div class="collapse navbar-collapse" id="navigation">
      <div class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
          <button {{action "signOut"}} class="nava">Sign Out</button>
        {{else}}
            {{#link-to "sign-up"}}Sign Up{{/link-to}}
            {{#link-to "sign-in"}}Sign In{{/link-to}}
        {{/if}}
    </div>
  </div>
  </div>
</nav>
