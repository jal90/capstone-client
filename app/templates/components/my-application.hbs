<nav class="navbar navbar-default nav-car-pic">
  <div class="container-fluid">
    {{navbar-header}}


    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          <li>
            {{#link-to "vehicles" class="header-db" }}{{format-user user}}'s Dashboard{{/link-to}}
          </li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
          <li class="nava" style="margin-right: 40px;">
            {{#link-to "change-password" }}Change Password{{/link-to}}
          </li>
          <li class="nava" ><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
          <li>
            {{#link-to "sign-up"}}Sign Up{{/link-to}}
          </li>
          <li>
            {{#link-to "sign-in"}}Sign In{{/link-to}}
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

{{!-- <div class="collapse navbar-collapse" id="navigation">
      {{#link-to "vehicles" class="header-db" }}{{format-user user}}'s Dashboard{{/link-to}}

      <ul class="nav navbar-nav navbar-right">
        <li>
          {{#link-to "new" class="nava create" }}Create a new vehicle{{/link-to}}
        </li>
        <li>
          <button class="nava">{{#link-to "change-password"}}Change Password{{/link-to}}
        </li>
        <li>
          <button {{action "signOut"}} class="nava">Sign Out</button>
        </li>
      </ul>
    </div>
  </div>
</nav> --}}
